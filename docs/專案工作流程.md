---
title: 2026 Codex 專案工作流程
date: 2026-05-02
type: cockpit
tags:
  - project
  - workflow
  - classroom-tools
---

# 2026 Codex 專案工作流程

> 進度、下一步、踩坑與收工紀錄放這裡。專案固定規則與入口看根目錄的 `AGENTS.md`。

## 上次做到哪

**最後動作：** 針對開工變慢問題精簡 `jacob-kb-vault` 團隊交接入口，將舊歷史歸檔並推送 commit `1a0f584`
**狀態：** 團隊交接主檔已瘦身；開工 SOP 改為優先讀「今日摘要 + 任務隊列 + 任務專案 AGENTS / _worklog」；OpenDesign 待接入；文字雲暫緩

## 下一步

1. ~~交辦 OpenCode 建立素材消化工作模式：`Templates/素材分析.md`、`知識庫/素材消化流程.md` 與入口檔引用。~~（已完成）
2. ~~Codex 驗收 OpenCode 產出：只檢查相關 Markdown、成功標準、驗證方式與是否有未完成項目。~~（已完成）
3. 將 OpenDesign 列為待接入角色；等有 CLI、connector 或官方安裝方式後再接入。
4. 下次有外部素材時，先用 `Templates/素材分析.md` 建立 `Clippings/` 分析文件，再整理到 `知識庫/`。

## 素材消化工作模式

### 已建立文件
- `Templates/素材分析.md`：素材分析標準模板
- `知識庫/素材消化流程.md`：完整消化流程指南

### 使用時機
當使用者貼影片、文章、GitHub repo 或外部資料時，先走素材消化流程：

1. 建立 `Clippings/` 素材分析文件
2. 使用 `Templates/素材分析.md` 標準格式
3. 消化後存到 `知識庫/`，並更新 `知識庫/index.md`
4. 相關流程見 `知識庫/素材消化流程.md`

### 入口檔引用
- `AGENTS.md`：Obsidian 協作區塊已加入素材消化流程指引
- `CLAUDE.md`：已加入使用者貼外部資料時的處理指引
- `GEMINI.md`：已加入素材消化流程說明

## 工具清單

| 工具 | 路徑 | 狀態 |
|---|---|---|
| 班級文字雲 | 待建立 | Firebase 已準備 `wordcloud_words` |

## 系統狀態

| 項目 | 狀態 |
|---|---|
| GitHub repo | `jacob-cloud-n8n/codex` 已連接 |
| Firebase project | `codex-jacob` 已連接 |
| Firestore | Standard，`asia-east1` |
| Firebase MCP | 已驗證可新增、讀取、列出、刪除文件 |
| startup-sync | 已建立 |
| shutdown-sync | 已建立 |
| project-init-sync | 已建立 |
| codex-youtube-video-workflow | 已安裝 |
| audio-to-srt | 已安裝 |
| smart-cut | 已安裝 |
| Gemini CLI | `0.41.2` 已安裝 |
| OpenCode CLI | `1.15.12` 已安裝，`opencode run` 最小測試通過 |
| Taste Skill 子集 | 已安裝 `taste-skill`、`redesign-skill`、`image-to-code-skill`、`imagegen-frontend-web`、`brandkit` |
| anthropic-skill-creator | 已安裝並驗證 |
| find-skills | 已安裝並驗證 |
| gstack | 觀察名單；非必要不安裝，若要使用需先做單一功能審查 |
| OpenDesign | 待接入；目前尚無可呼叫 CLI / connector |

## 最近更動紀錄

| 日期 | 變更摘要 |
|---|---|
| 2026-05-02 | 建立 #07 專案駕駛艙與全域工作流程 skills |
| 2026-05-02 | 完成 Firebase MCP 驗證 |
| 2026-04-28 | 完成 Codex skills / plugins 快篩與 GitHub CLI 登入 |
| 2026-04-26 | 建立 React + Vite + TypeScript 專案、GitHub 與第二大腦結構 |
| 2026-05-10 | 從 `mathruffian-dot/2026-YouTube` 安裝 YouTube workflow skills |
| 2026-05-11 | 安裝 Gemini CLI 並新增 `GEMINI.md` |
| 2026-05-11 | 收工：第二大腦、Gemini CLI、GitHub 同步完成 |
| 2026-05-12 | 開工：安裝依賴並通過 `npm run build` |
| 2026-05-12 | 安裝 Anthropic `skill-creator`，以 `anthropic-skill-creator` 避免和系統 skill 衝突 |
| 2026-05-12 | 安裝 Vercel `find-skills`，並確認 `npx skills find` 可搜尋技能 |
| 2026-05-12 | 審查 `garrytan/gstack`：判定不是單一 skill，而是包含瀏覽器、自動化、telemetry、記憶同步與多個工具腳本的完整工具包，暫不安裝完整版 |
| 2026-05-12 | 收工：完成 `gstack` 觀察名單規則，GitHub 已同步 |
| 2026-05-19 | 依 `CLAUDE-md-12rules.md` 導入 `docs/development-quality-rules.md`，並讓 `AGENTS.md`、`CLAUDE.md`、`GEMINI.md` 引用 |
| 2026-05-19 | 收工：文字雲暫緩，開發品質 12 條規則已導入並同步到 GitHub |
| 2026-05-30 | 安裝 OpenCode CLI `1.15.12`，修正 `opencode.jsonc` 使用 `{env:MIMO_API_KEY}`，確認 `opencode run` 最小測試通過 |
| 2026-05-30 | 確認團隊分工模式：Jacob 發令、Codex 統籌與驗收、OpenCode 執行明確任務；OpenDesign 列為待接入 |
| 2026-05-30 | 交辦 OpenCode 建立素材消化工作模式，Codex 驗收並微調素材分析模板 |
| 2026-05-30 | 收工：素材消化工作模式已提交並推送到 GitHub，工作區乾淨 |
| 2026-06-05 | 收工：已在 `jacob-kb-vault/團隊交接.md` 填入 Codex 對 Agentic-Skills、Superpowers 與長期記憶架構的建議，commit `afe14b2` 已推送 |
| 2026-06-06 | 驗收 `jacob-kb-vault` 已新增 `客製化變更與合併指引.md`、`決策演進日誌.md`，並將規則升級為全域開發 13 規則 |
| 2026-06-06 | 審查並安裝 `Leonxlnx/taste-skill` 適用子集；未安裝過度激進或風格限定的 skill |
| 2026-06-06 | 收工：確認 Taste Skill 已登記於 `jacob-kb-vault` 資源庫、AGENTS 技能清單與決策日誌 D006 |
| 2026-06-10 | 確認開工變慢主因是 `jacob-kb-vault/團隊交接.md` 膨脹至約 33KB；已將主檔瘦身至約 5KB、建立 `團隊交接-歷史.md`、精簡 `Wiki駕駛艙.md` 並推送 commit `1a0f584` |

## 踩坑筆記

- Firebase standalone CLI 第一次啟動需要寫入 `~/.cache/firebase`，在 Codex 沙箱中需要授權。
- `firestore_add_document` 的 `document` 參數要傳 Firestore document 物件，不要包成 JSON 字串。
- Codex 改完 MCP 設定後，要完整重啟 Desktop 才會載入工具。
- 終端機與 Codex 的 PATH 可能不同；必要時用完整路徑 `/Users/jacob/.local/bin/firebase`。
- Gemini CLI 第一次執行會建立 `~/.gemini`；若在沙箱中報權限錯誤，先建立該資料夾再驗證。
- `garrytan/gstack` 安裝流程會寫入 `~/.codex/skills`、`~/.gstack`，建置 Playwright/Bun 工具，並包含 telemetry、瀏覽器 cookie 匯入、gbrain/記憶同步與私有 repo artifact sync；列入觀察名單，非必要不安裝。若未來確實需要，只審查並安裝單一必要功能，不安裝完整版。
- OpenCode 在非互動 shell 不會自動載入 `~/.zshrc`，需要用 `zsh -ic 'opencode run ...'` 才能讀到 `MIMO_API_KEY`。
- OpenCode 設定檔的環境變數格式要用 `{env:MIMO_API_KEY}`，不要用 `${MIMO_API_KEY}`。
- OpenCode 執行會呼叫外部模型；交辦前需確認任務可讓外部 provider 讀取，避免送出 API key、學生姓名或敏感資料。
- Firebase/firepit 可能產生 `firepit-log.txt` 工具日誌，已加入 `.gitignore`，不要提交。
- 團隊交接檔若長期累積歷史、完整架構圖與全員長篇回覆，會拖慢開工；主檔只保留今日摘要、任務隊列與精簡 SOP，舊內容歸檔到 `團隊交接-歷史.md`。

## 開工流程

說「開工」時：

1. 讀 `AGENTS.md`
2. 讀本檔
3. 檢查 Git 狀態
4. 檢查 Firebase MCP 狀態
5. 回報下一步

## 收工流程

說「收工」時：

1. 盤點本次工作
2. 更新本檔
3. 檢查 Git diff
4. 只提交本次相關檔案
5. push 到 GitHub
